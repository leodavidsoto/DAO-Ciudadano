# CRA 5's legacy optional TypeScript/ESLint peer ranges conflict with this
# existing frontend toolchain. Runtime peers are still verified explicitly in
# CI/tests; this setting can disappear when CRA is migrated.
legacy-peer-deps=true
