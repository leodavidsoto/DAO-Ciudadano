# DAO Ciudadana — Backend (DESARROLLO Y CI)
# Producción + herramientas de test y calidad.
#   pip install -r requirements-dev.txt

-r requirements.txt

# === Testing ===
pytest==8.3.4
pytest-asyncio==0.24.0
mongomock-motor==0.0.36    # MongoDB async en memoria, sin contenedor
httpx==0.28.1              # AsyncClient en tests/conftest.py
# El extra [lua] es imprescindible: sin el, fakeredis no ejecuta el script de
# ventana deslizante y el camino Redis quedaria sin probar.
fakeredis[lua]==2.37.0

# === Calidad de código ===
black==24.10.0
flake8==7.3.0
mypy==1.14.0

# === Cadena de suministro ===
pip-audit==2.10.1
pytest-cov==7.1.0
coverage==7.15.3
