<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Lectura del chip de la cédula (ROADMAP 4.2).
         `react-native-nfc-manager` ya aporta este permiso por manifest
         merging, así que declararlo aquí no arregla nada hoy: lo hace
         explícito. `PassportReaderModule` usa NfcAdapter directamente y
         seguiría necesitándolo aunque esa librería se quitara del proyecto. -->
    <uses-permission android:name="android.permission.NFC" />
    <!-- required=false a propósito: la app sigue siendo instalable y útil en
         teléfonos sin NFC (gobernanza, wallet); solo la verificación de
         identidad queda fuera de alcance, y el módulo lo reporta con
         E_NFC_UNAVAILABLE en vez de fallar de forma opaca. -->
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
