{
  "name": "dao-ciudadana-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circuitos ZKP (Circom/Groth16) para la identidad soberana de DAO Ciudadana",
  "scripts": {
    "build": "bash compile.sh",
    "compile": "circom2 verify_identity.circom --r1cs --wasm --sym --inspect -l node_modules -o build",
    "fixture": "node scripts/generate-test-fixture.js",
    "manifest": "node scripts/generate-artifact-manifest.js",
    "test": "node test/membership-circuit.test.js"
  },
  "devDependencies": {
    "circom2": "0.2.23",
    "circomlib": "2.0.5",
    "circomlibjs": "0.1.7",
    "snarkjs": "0.7.6"
  },
  "overrides": {
    "ws": "8.21.1"
  }
}
