# INSTRUCCIONES DE INICIO PARA CLAUDE

El usuario acaba de reiniciar tu sesión de terminal. 

**Estado Actual:**
Acabas de terminar exitosamente la ceremonia de Tally (ptau power 17), desplegaste el `TallyVerifier` en Sepolia y llamaste a `setTallyVerifier` en el `MACICoordinator`. Todo eso está completado y documentado en `HANDOFF.md` y `ROADMAP.md`.

**Tu tarea actual:**
1. Revisa tu estado y el de los repositorios. No hay tareas pendientes inmediatas de contratos por tu lado, pero el usuario está por generar su llave MACI (con el script de Python) y llamar a `setCoordinatorPubKey` en Sepolia.
2. Mantente a la espera para ayudar al usuario con este paso si tiene problemas o para verificar que la llave pública haya quedado correctamente seteada en la blockchain.
3. Recuerda SIEMPRE respetar los límites de tu dominio (Backend y Contratos). No toques Frontend ni Mobile.
